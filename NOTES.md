# NOTES

WHY: Idiom critic returned no verdict (its 5-idiom-1 invocation crashed/timed out); the diff is 6
files / 278 lines, so I treated the "too large to review" guess as not applicable and re-ran the full
gate instead — nothing to fix in code.